# Pulse ERP Environment Configuration
# Raspberry Pi 5 Deployment

# Domain Configuration (using .local for local network)
DOMAIN=pulse-erp.local

# Database Configuration
POSTGRES_DB=pulse_erp
POSTGRES_USER=pulseadmin
POSTGRES_PASSWORD=changeme123

# Grafana Configuration
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin123

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123

# ACME/LetsEncrypt (for production with real domain)
ACME_EMAIL=admin@example.com

# Data Path (use SSD if available)
DATA_PATH=./data

# API URLs (updated ports for frontend - use Pi IP for browser access)
# Change this IP if your Pi's IP changes
PI_IP=192.168.1.199
NEXT_PUBLIC_API_BASE_URL=http://${PI_IP}:8011
NEXT_PUBLIC_INVENTORY_API_URL=http://${PI_IP}:8012
NEXT_PUBLIC_BILLING_API_URL=http://${PI_IP}:8013
NEXT_PUBLIC_OLAP_API_URL=http://${PI_IP}:8014
NEXT_PUBLIC_GRAFANA_URL=http://${PI_IP}:3010
